import MapR from './Map';

describe('map', () => {
    // it('check map boundarie', () => {
    //     const map = new MapR(1,1)
    //     const actual = map.isValidPosition({x:0,y:0})
    //     expect(actual).toBeTruthy()
    // });

    // it('check map boundarie', () => {
    //     const map = new MapR(1,1)
    //     const actual = map.isValidPosition({x:4,y:0})
    //     expect(actual).toBeFalsy()
    // });
});
